# Leaf-Detection-using-tensorflow_object_detection-api
Go through the Tensorflow object detection api for basic understanding. then make use of the trained model I provided for leaf detection.
i have used labelmap to label the images for training
the model is pre trained to detect leaf ready to use.
